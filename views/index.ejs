<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Practice</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
     <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body>
    <div class ="main w-full min-h-screen bg-zinc-900">
          <div class =" form p-10 text-white">
               <form action="/create" method="POST" >
                    <input class="block w-full px-5 py-2 bg-zinc-800 text-white rounded-lg" name="details" class="input-field" placeholder="Enter a task">
                    <textarea class="block w-full mt-2 px-5 py-2 bg-zinc-800 text-white rounded mt-2 resize-none" name="content" placeholder="write your task details here"></textarea>
                    <input type="submit" value="Create task" class="block  px-5 py-2 bg-blue-600 text-white rounded mt-2">
               </form>
          </div>
          <div class ="tasks flex gap-3 flex-wrap p-10">
            <% if (files && files.length > 0) { %>
            <% files.forEach(function(val) {    %>
                <div class="task bg-zinc-800 text-white px-5 w-72 rounded-lg">
                      <h2 class="text-2xl text-white font-bold"><%= val %></h2>
                      <div class ="flex gap-6">
                      <a class="text-blue-500 inline-block mt-3"href="/files/<%= val %>"> Read more</a>
                      <a class="text-zinc-500 inline-block mt-3"href="/edit/<%= val %>"> Edit</a>
                      </div>
          </div>
          <% }); %>
          <% } else { %>
          <h3 class=" text-zinc-600"> No task yet </h3>
            <% } %>
    </div>

    
</body>
</html>